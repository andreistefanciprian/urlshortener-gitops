apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: url-read
  namespace: url-read
spec:
  interval: 5m0s
  releaseName: url-read
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      remediateLastFailure: true
  chart:
    spec:
      chart: url-read
      version: "0.1.11"
      sourceRef:
        kind: HelmRepository
        name: url-read
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/andreistefanciprian/url-read
      tag: 1.2.2@sha256:a70e61b4dadb700d7b8412476f0e9b4b9ef1d16787d6b3b27610f97c0f58fbeb
    config:
      LOG_LEVEL: DEBUG
      DB_HOST: pg-postgresql.postgres.svc
      REDIS_HOST: redis-master.redis.svc
      REDIS_DB: "0"
      DB_USER: url_gen_user
    secrets:
      db:
        secretName: pg-creds
        passwordKey: user-password
      redis:
        secretName: redis-creds
        passwordKey: password
